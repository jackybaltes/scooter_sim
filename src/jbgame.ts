import JBScene from './jbscene';

class JBGame {

    scenes = new Array<JBScene>();

    constructor( ) {

    }

    addScene( scene : JBScene ) {
        this.scenes.push( scene );
    }

    run( ) {
        while( ! done ) {
            
        }
    }
}

export default { JBGame };